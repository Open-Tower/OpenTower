# OpenTower Linux Ops 比赛设计说明

## 1. 项目概述

OpenTower Linux Ops 是一个面向 Linux 服务器运维场景的自然语言智能代理系统。系统以命令行工具为主要交互入口，允许用户直接使用自然语言描述需求，由系统自动完成意图识别、安全判定、命令规划、执行与结果反馈。

本项目对应 `AI_Hackathon_2026.pdf` 与 `技术组移植任务说明.md` 中对“操作系统智能代理”的要求，重点覆盖以下目标：

- 自然语言驱动 Linux 基础运维操作
- 高风险操作识别、阻断与二次确认
- 多 agent 分工协作，但执行链路保持稳定、可解释、可验证
- 在真实 Linux 环境中具备可运行、可演示、可审计的能力

当前系统围绕固定四 agent 工作流构建：

- `intent-parser`
- `security-guard`
- `command-planner`
- `result-analyst`

这种设计不是把“多 agent”当作展示概念，而是把它用于明确职责边界，保证每一轮执行都能被拆解、记录和解释。

## 2. 方案定位

这套方案的核心思路不是“让大模型自由决定一切”，而是“让自然语言交互进入系统运维场景，同时保留工程可控性”。因此它有三个明确定位：

### 2.1 真实可用

- 不是纯 Prompt 展示
- 不是概念性架构图
- 不是仅返回建议命令

系统已经具备真实 CLI 入口、真实命令执行、真实日志输出、真实确认流和自动化测试。

### 2.2 安全优先

系统操作不同于普通问答。比赛场景里，如果让模型直接拼接命令，虽然看起来“更智能”，但演示不稳定，且极易产生高风险误操作。本方案把安全检查放在命令执行之前，先决策再执行。

### 2.3 多 agent 有实际职责，不是形式拼接

四个 agent 的拆分分别对应：

- 理解用户想做什么
- 判断这件事能不能做、风险多大
- 把安全后的意图转成受控命令
- 把命令结果转回自然语言

这种拆分使各层的输入、输出和职责边界保持清晰，而不是把所有能力压成一个黑箱。

## 3. 架构设计

## 3.1 总体架构

系统采用 CLI-first 架构，运行主线如下：

```text
用户自然语言输入
  -> CLI / Console 入口
  -> intent-parser
  -> security-guard
  -> command-planner
  -> linux_executor
  -> result-analyst
  -> 自然语言反馈 + 日志/转录/输出文件
```

其中：

- CLI 是正式执行入口
- Console 是持续交互入口
- `/` 开头输入表示显式命令
- 非 `/` 输入默认按自然语言任务处理

## 3.2 分层设计

从工程实现上看，系统可分为四层：

### 交互层

负责接收输入与展示输出，包括：

- `python -m opentower_cli ...`
- `python -m opentower_cli console`
- `/workflow`、`/provider-status`、`/auth` 等显式命令

这一层的特点是：

- 用户入口简单
- 自然语言优先
- 显式命令只在需要时出现

### 编排层

负责把一次请求拆成多 agent 工作流，包括：

- 请求分发
- 工作流装配
- 确认流状态管理
- 运行日志与产物路径组织

这层确保每一轮执行都能形成完整闭环，而不是只看最终输出。

### 执行层

负责 Linux 命令执行，包括：

- 本地 bash / WSL 执行
- 超时控制
- 返回码处理
- 输出收集

这一层明确与自然语言理解层分离，避免模型直接接管执行环境。

### 持久化与审计层

负责保存：

- session log
- transcript
- final output
- group chat recap
- confirmation record

这层是系统可审计、可复现的关键。

## 3.3 目录结构与职责

- `runtime/opentower_cli/`
  - 系统运行时代码
- `org/system.yaml`
  - 系统配置、agent 列表、执行参数
- `org/skills.yaml`
  - 工作流列表、分类、校验项、示例请求
- `.opentower/agents/`
  - agent 定义
- `.opentower/skills/`
  - 工作流定义
- `production/`
  - 日志、转录、确认状态、输出文件
- `tests/`
  - 自动化测试

## 4. 四个 agent 的职责拆分

## 4.1 Intent Parser

对应文件：`runtime/opentower_cli/intent_parser.py`

职责：

- 将自然语言路由到支持的工作流
- 抽取结构化实体
- 识别读操作与写操作
- 识别高风险意图前的操作对象

当前可抽取的关键实体包括：

- 路径
- 端口
- 用户名
- 用户组
- 文件名模式
- 搜索类型
- 服务名

为什么单独拆成这一层：

- 运维任务是否安全，首先取决于“理解得对不对”
- 如果意图识别与命令生成耦合在一起，出错时很难定位问题来源
- 单独拆出 parser 后，测试可以直接覆盖“输入自然语言 -> 输出结构化意图”

## 4.2 Security Guard

对应文件：`runtime/opentower_cli/security_agent.py`

职责：

- 对解析后的意图做风险判断
- 对极高风险操作直接阻断
- 对高风险操作进入确认流
- 给出风险原因与潜在影响说明

当前风险决策分为三类：

- `allow`
- `confirm`
- `block`

这个层的价值在于：

- 不把安全寄托在模型“自己别犯错”
- 不把危险操作留到执行阶段再补救
- 风险提示是结构化输出，不是随意文本

## 4.3 Command Planner

对应文件：`runtime/opentower_cli/command_generator.py`

职责：

- 把安全通过后的结构化意图转成 Linux 命令计划
- 区分预览命令与执行命令
- 限制命令集合在比赛需求范围内

设计原则：

- 先缩小命令面，再提高稳定性
- 明确文件搜索与目录搜索
- 明确 destructive 操作与 inspect 操作

相比“让模型直接输出 shell”，当前方案的优势是：

- 可预测
- 容易测试
- 更利于稳定运行与验证

## 4.4 Result Analyst

对应文件：`runtime/opentower_cli/feedback_agent.py`

职责：

- 解析命令输出
- 把原始结果整理成自然语言反馈
- 对磁盘、搜索、进程、用户管理分别做定制化总结
- 在确认流里生成面向用户的下一步说明

这一层直接影响系统反馈质量：

- 输出不是生硬的原始命令结果
- 也不是脱离实际的“大模型润色”
- 而是带结构、可复核的自然语言反馈

## 5. 关键工作流说明

## 5.1 磁盘使用情况监测

支持的请求示例：

- `查看磁盘使用情况`
- `哪个分区快满了`
- `检查服务器存储空间`

主要命令：

- `df -h`
- `lsblk`
- `du -sh /var/log/*`

输出特点：

- 自动总结挂载点
- 自动识别高占用分区
- 返回自然语言结果而不是原始表格堆叠

## 5.2 文件或目录检索

支持的请求示例：

- `找到所有 nginx 配置文件`
- `查找 /var/log 下的所有错误日志`
- `搜索包含 "database" 的文件`
- `查找 cache 目录`

主要命令：

- `find ... -type f -name ...`
- `find ... -type d -name ...`
- `grep -R -n -i ...`

输出特点：

- 区分文件搜索和目录搜索
- 区分按文件名检索和按内容检索
- 对常见配置场景有合理默认搜索范围

## 5.3 进程及端口状态查询

支持的请求示例：

- `哪些进程占用 80 端口`
- `查看 nginx 服务状态`
- `显示占用内存最多的 5 个进程`

主要命令：

- `lsof -i :80`
- `ss -ltnp`
- `systemctl status`
- `ps aux --sort=-%mem`

输出特点：

- 支持端口、进程、服务三类查询
- 对常见服务名如 nginx、docker 有直接识别能力

## 5.4 用户管理

支持的请求示例：

- `创建一个名为 dev01 的用户`
- `给用户 dev01 加入 docker 组`
- `删除用户 test_user`
- `查看所有用户`

主要命令：

- `useradd -m -s /bin/bash`
- `usermod -aG`
- `userdel -r`
- `getent passwd`

输出特点：

- 支持创建、删除、加组、查看
- 对批量删除采用“先预览，后确认”的模式

## 6. 安全机制设计

## 6.1 为什么安全机制是本方案的重点

比赛题目不是单纯做一个聊天机器人，而是让 AI 真正进入服务器操作系统场景。这个场景下，安全能力本身就是产品能力的一部分，而不是附属功能。

因此本方案把安全放在核心执行路径中，而不是作为额外提示。

## 6.2 直接阻断

对于以下请求，系统直接拒绝执行：

- 删除 `/etc`、`/boot`、`/sys`、`/proc` 等关键目录
- 删除受保护账户，如 `root`

这一类请求的设计目标不是“提醒一下”，而是“立即停止”。

## 6.3 二次确认

对于高风险但不是立即阻断的操作，系统进入确认流，包括：

- 删除普通路径
- 递归权限变更
- 批量删除用户

确认输出包含：

- `confirmation_id`
- 风险等级
- 风险原因
- 潜在影响
- 继续执行命令
- 取消执行命令

该流程能够清楚体现：

- 系统知道这是危险操作
- 系统不会直接执行
- 系统会把理由说清楚
- 系统给出明确的可追踪确认过程

## 6.4 审计闭环

系统在 `production/` 下维护以下产物：

- `session-logs/`
- `session-outputs/`
- `session-transcripts/`
- `group-chat-recaps/`
- `pending-confirmations/`

确认完成后，系统会把最终结果回写到主运行日志，保证外部查看 run log 时不会停留在“pending_confirmation”的中间状态。

这个设计带来的效果是：

- 方案不是一次性演示脚本
- 具备连续使用和复盘基础
- 每一次危险操作都有可追踪证据链

## 7. 配置与 LLM 接入方式

## 7.1 `auth.json`

系统默认从仓库根目录读取 `auth.json` 作为 LLM / provider 配置文件。仓库同时提供 `auth.example.json` 作为可提交模板，推荐复制为 `auth.json` 后再填写。如果文件不存在，首次启动也会自动生成模板。

其中：

- `auth.example.json` 用于仓库分发与格式说明
- `auth.json` 用于本地真实配置
- `auth.json` 已加入 `.gitignore`，不会进入版本库

当前兼容两种格式：

### 简单环境变量式

```json
{
  "OPENAI_API_KEY": "YOUR_OPENAI_COMPATIBLE_API_KEY",
  "OPENAI_BASE_URL": "https://api.deepseek.com/v1"
}
```

### 标准模板式

```json
{
  "active_provider": "openai-compatible",
  "providers": {
    "openai-compatible": {
      "model": "gpt-4o-mini",
      "api_base_url": "https://api.openai.com/v1",
      "api_key": "YOUR_OPENAI_COMPATIBLE_API_KEY"
    }
  }
}
```

推荐使用步骤：

1. 复制 `auth.example.json` 为 `auth.json`
2. 按实际 provider 修改 `active_provider`
3. 填写对应的 `model`、`api_base_url` 与 `api_key`
4. 运行 `/auth` 与 `/provider-status` 检查读取状态

## 7.2 支持的 provider

当前支持：

- `anthropic`
- `openai-compatible`
- `ollama`

其中：

- `anthropic` 适合云端模型
- `openai-compatible` 适合 DeepSeek / OpenAI / OpenRouter / SiliconFlow 等兼容接口
- `ollama` 适合本地模型

## 7.3 配置查看方式

可以通过：

```bash
python -m opentower_cli /auth
python -m opentower_cli /provider-status
```

查看：

- 当前读取的 `auth.json` 路径
- 当前生效 provider
- 当前 base URL
- API key 是否存在
- provider 是否具备可执行条件

## 8. 用法说明

## 8.1 顶层 CLI：自然语言优先

现在顶层 CLI 默认优先处理自然语言，不需要先写 `dispatch`。

例如：

```bash
python -m opentower_cli 查看磁盘使用情况
python -m opentower_cli 找到所有 nginx 配置文件
python -m opentower_cli 哪些进程占用 80 端口
python -m opentower_cli 创建一个名为 dev01 的用户并加入 docker 组
```

这种形式更接近“操作系统自然语言入口”，而不是“一个套着自然语言参数的命令工具”。

## 8.2 `/` 开头命令：显式控制模式

当用户明确需要 CLI 控制时，可以使用 `/` 开头的命令：

```bash
python -m opentower_cli /workflow
python -m opentower_cli /provider-status
python -m opentower_cli /auth
```

设计理由：

- 默认交互是自然语言
- 显式控制是补充能力
- 两种模式边界清晰，不会混淆

## 8.3 交互式 Console

启动方式：

```bash
python -m opentower_cli console
```

Console 的规则是：

- 默认输入都是自然语言请求
- 只有以 `/` 开头时才按显式命令解释

示例：

```text
查看磁盘使用情况
找到所有 nginx 配置文件
/workflow
/provider-status
/auth
```

## 8.4 确认流

高风险操作触发后，系统不会直接执行，而是返回 `confirmation_id`。

继续执行：

```bash
python -m opentower_cli dispatch --confirmation-id <id> --answer yes --reason "approved maintenance"
```

取消执行：

```bash
python -m opentower_cli dispatch --confirmation-id <id> --answer no
```

## 9. 方案特点与优势

## 9.1 真实感强

很多参赛方案容易停留在“自然语言转命令建议”。本方案不是建议，而是完整的执行链：

- 有真实命令
- 有真实日志
- 有真实确认流
- 有真实输出文件

这让方案更像“可工作的系统”，而不是“讲得通的思路”。

## 9.2 风险可控

系统管理场景里，通常会关注两个问题：

1. 会不会乱执行
2. 错了之后能不能解释

本方案在这两点上都有清晰回答：

- 危险操作先判断再执行
- 高风险操作必须确认
- 每个处置都有原因说明

## 9.3 执行稳定性

纯模型自由生成命令的执行波动和操作风险较高。当前方案将执行面收敛在固定范围内，带来以下工程收益：

- 可预测
- 可提前测试
- 边界清晰
- 不容易因为临场 Prompt 漂移导致错误

这使系统更适合在受限范围内进行稳定验证。

## 9.4 多 agent 拆分有工程价值

本方案的多 agent 不是视觉包装，而是明确分工：

- parser 负责理解
- security 负责风险
- planner 负责命令
- analyst 负责反馈

这种拆分同时满足了多 agent 协作形态与工程边界可控两项要求。

## 9.5 易于扩展

虽然当前范围聚焦比赛核心需求，但系统已经具备扩展接口：

- provider 可替换
- 执行层可扩展到 SSH
- 交互层可扩展到 TUI / Web
- 规则层可继续扩展更多 Linux 运维任务

这说明方案不仅能参加比赛，也具备向真实产品化继续演进的结构基础。

## 10. 验证方式与示例场景

## 10.1 自动化测试

仓库内已有自动化回归测试，覆盖：

- 意图解析
- 命令规划
- Console 路由
- `auth.json` 读取
- 自然语言顶层 CLI
- 确认流状态回写
- Windows / WSL 执行兼容

测试命令：

```bash
python -m pytest -q
```

当前结果：

- `46 passed`

## 10.2 示例验证场景

可按以下顺序组织验证流程：

1. `查看磁盘使用情况`
2. `找到所有 nginx 配置文件`
3. `哪些进程占用 80 端口`
4. `创建一个名为 dev01 的用户并加入 docker 组`
5. `删除 /etc 目录`，展示系统阻断
6. `给所有文件 777 权限`，展示二次确认
7. `删除所有测试用户`，展示预览加确认流程
8. `/provider-status`，展示配置状态可见
9. `/auth`，展示 `auth.json` 已接入

以上场景可以覆盖基础能力、安全能力和可解释能力。

## 11. 当前边界

当前系统明确不追求“大而全”，而是优先把比赛核心要求做实。当前边界如下：

- 聚焦比赛要求的 Linux 运维场景
- 默认本地执行
- 未正式启用 SSH 远程执行
- 未提供语音输入
- 未提供 Web UI

这些边界是主动取舍，不是能力缺口失控。

## 12. 结语

OpenTower Linux Ops 的重点不在于“让模型显得更自由”，而在于把自然语言交互真正落到 Linux 运维场景里，同时保留系统级操作所必需的可控性、可解释性和可验证性。

从方案表现看，这套系统的主要特点体现在：

- 功能范围清楚
- 风险控制明确
- 多 agent 分工真实
- 交互方式自然
- 执行链路完整
- 执行稳定，工程实现完整

在真实 Linux 环境下，可按上述场景进行验证，以覆盖主要能力点。
